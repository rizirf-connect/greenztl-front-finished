import { Spin } from "antd";
import React from "react";

const DataLoader = () => {
  return (
    <Spin className="h-72 !flex items-center justify-center" size="large" />
  );
};

export default DataLoader;
